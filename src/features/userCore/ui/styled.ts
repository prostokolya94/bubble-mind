import { styled } from 'styled-components';

export const BubbleWrapper = styled.div`
    display: flex;
    background: red;
    border-radius: 50%;
    width: 100px;
    height: 100px;
    cursor: pointer;
`;
